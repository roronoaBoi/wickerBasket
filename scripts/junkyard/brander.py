"""
script to pick new cs brand
"""

import random

choices = [
    "gardeners of value",
    "we are the glue",
    "outcomes over optics",
    "when they win, we win"
]

the_choice = random.choice(choices)

print(the_choice)
